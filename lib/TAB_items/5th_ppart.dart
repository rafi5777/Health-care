import 'package:flutter/material.dart';
class Tab_5 extends StatelessWidget {
  const Tab_5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('5th Part'),);
  }
}