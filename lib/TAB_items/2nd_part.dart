import 'package:flutter/material.dart';
class Tab_2 extends StatelessWidget {
  const Tab_2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('2nd Part'),);
  }
}