import 'package:flutter/material.dart';
class Tab_3 extends StatelessWidget {
  const Tab_3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('3rd Part'),);
  }
}